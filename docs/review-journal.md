# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its systems programming focus without claiming live deployment or external usage.

## Cases

- `baseline`: `allocation pressure`, score 180, lane `ship`
- `stress`: `dirty state`, score 148, lane `ship`
- `edge`: `guard slack`, score 157, lane `ship`
- `recovery`: `layout drift`, score 175, lane `ship`
- `stale`: `allocation pressure`, score 176, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
